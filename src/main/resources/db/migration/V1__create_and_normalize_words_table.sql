CREATE TABLE IF NOT EXISTS words (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    word VARCHAR(50) NOT NULL,
    type VARCHAR(12)
);

ALTER TABLE words ADD COLUMN IF NOT EXISTS word VARCHAR(50);
ALTER TABLE words ADD COLUMN IF NOT EXISTS type VARCHAR(12);

ALTER TABLE words ALTER COLUMN word SET NOT NULL;

SELECT setval(
    pg_get_serial_sequence('words', 'id'),
    COALESCE((SELECT MAX(id) FROM words), 0) + 1,
    false
);